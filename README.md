Sources for the plugin `plugin.tinyfiledialogs`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.tinyfiledialogs" = {
            publisherId = "com.xibalbastudios",
        },
    },
}
```
